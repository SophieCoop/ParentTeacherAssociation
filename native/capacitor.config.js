/* ============================================================
   המעטפת הנייטיבית — הגדרה אחת לאייפון ולאנדרואיד
   ------------------------------------------------------------
   קובץ JS ולא TS: ה-CLI טוען אותו ב-require, כך שאפשר לכתוב בו
   הערות בלי להוסיף TypeScript לפרויקט.

   אין כאן server.url: קבצי האתר נארזים בתוך האפליקציה ומתחלפים
   בעדכונים חיים, ואינם נטענים מ-vaadhorim.com. אפליקציה שרק טוענת
   אתר נפתחת לדף שגיאה בלי רשת, ואפל דוחה אותה (סעיף 4.2).
   ============================================================ */
module.exports = {
  appId: 'com.vaadhorim.app',
  // השם שמתחת לאייקון — כמו short_name במניפסט של האתר
  appName: 'ועד הורים',
  webDir: 'www',
  // צבע ה-WebView לפני שהעמוד מצויר, כדי שלא יהבהב לבן
  backgroundColor: '#FBF8F3',

  /* המקור של העמוד הוא הבעלים של localStorage, ואצל מי שעובד בלי
     חשבון זה העותק היחיד של הנתונים. כל שינוי כאן — או שינוי ברירת
     מחדל בגרסה עתידית של Capacitor, כמו שקרה ב-androidScheme בגרסה 6 —
     היה משאיר את הנתונים מאחור במקור הישן. לכן הערכים מקובעים. */
  server: {
    hostname: 'localhost',
    iosScheme: 'capacitor',
    androidScheme: 'https',
  },

  ios: {
    // העמוד מסדר את עצמו לפי env(safe-area-inset-*); ה-WebView לא מוסיף שוליים משלו
    contentInset: 'never',
  },
  android: {
    allowMixedContent: false,
  },

  plugins: {
    SplashScreen: {
      /* המסך מוסתר מהעמוד עצמו, אחרי הציור הראשון (או אחרי 3 שניות,
         המוקדם מביניהם) — כך אין רגע של מסך ריק בין השניים */
      launchAutoHide: false,
      launchFadeOutDuration: 150,
    },
    SystemBars: {
      /* העמוד תמיד בהיר. DEFAULT היה עוקב אחרי מצב כהה של הטלפון,
         ומצייר אייקונים לבנים על רקע בהיר — בלתי נראים */
      style: 'LIGHT',
      // אנדרואיד 15+ מצויר מקצה לקצה; המשתנים שמוזרקים משמשים גיבוי ל-env()
      insetsHandling: 'css',
    },
    LocalNotifications: {
      smallIcon: 'ic_stat_vaad',
      iconColor: '#8574D6',
      /* כשהאפליקציה פתוחה לא קופצת התראה: הפס שבמסך הבית כבר מציג
         את אותה תזכורת */
      presentationOptions: [],
    },
    CapacitorUpdater: {
      /* מצב ידני: העמוד שואל את האתר אם יש חבילה חדשה ומחליט מתי
         להתקין אותה. שום סטטיסטיקה לא נשלחת לשום מקום. */
      autoUpdate: false,
      statsUrl: '',
      // חבילה שלא הודיעה שעלתה תוך 10 שניות — מתגלגלת אחורה
      appReadyTimeout: 10000,
      // גרסה נייטיבית חדשה מתחילה מהקבצים שארוזים בה, לא מחבילה שירדה לגרסה הקודמת
      resetWhenUpdate: true,
    },
  },
};
