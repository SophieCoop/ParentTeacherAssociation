<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ערכת הנושא אחרי מסך הפתיחה. בהירה תמיד (לא DayNight), כי העמוד
         בהיר תמיד. רקע החלון בצבע של מסך הפתיחה ושל האתר, כדי שהרגע
         שבין מסך הפתיחה לציור הראשון של ה-WebView לא יהבהב. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- מסך הפתיחה: הכרטיס במרכז, על צבע הרקע של האתר. אנדרואיד 12+ מצייר
         אותו בעצמו, ותוסף מסך הפתיחה משאיר אותו עד שהעמוד מודיע שצויר. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_mark</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
