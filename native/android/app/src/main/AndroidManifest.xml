<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- גיבוי מופעל, בניגוד לפטק: אצל מי שעובד בלי חשבון, הנתונים נמצאים רק
         בטלפון, והם צריכים לעבור איתו לטלפון הבא. הכללים מגבים קובץ אחד בלבד —
         העותק שהעמוד שומר (vaad-gan-backup.json) — ולא את אחסון ה-WebView,
         שבו גם אסימון ההתחברות וגם חבילות העדכון. -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- שיתוף קובץ ה-Excel ותמונת הרעיון: הקבצים נכתבים למטמון בלבד,
             ורק הוא נחשף לאפליקציה שמקבלת את השיתוף -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- תוסף ההתראות מבקש התראות מדויקות לדקה, ו-Google Play מתירה את ההרשאה
         הזו רק לשעונים מעוררים וליומנים — אפליקציה אחרת נדחית. תזכורת "בעוד
         שבוע" לא צריכה דיוק של דקה, ובלי ההרשאה התוסף עובר להתראה לא־מדויקת. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
</manifest>
