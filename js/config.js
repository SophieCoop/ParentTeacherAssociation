/* ============================================================
   הגדרות חיבור לענן (Supabase)
   ------------------------------------------------------------
   המפתח כאן הוא מפתח ציבורי (publishable) שנועד לרוץ בדפדפן —
   הוא אינו סוד. ההגנה על הנתונים היא במדיניות ה-RLS בצד השרת,
   שמאפשרת לכל משתמש לקרוא ולכתוב אך ורק את השורה שלו.

   כדי להריץ את האפליקציה מקומית בלבד, בלי ענן — לרוקן את url.
   ============================================================ */
/* מספר הגרסה מוצג בהגדרות — כך אפשר לוודא שהמכשיר טען את הקוד העדכני */
var APP_VERSION = '2026.09.17h';

/* הטלפון שאליו נפתחת שיחת הוואטסאפ מהכפתור שבהגדרות ⚙️ → עזרה.
   מספר ישראלי רגיל, למשל '0501234567'.
   שימו לב: המספר גלוי לכל מי שנכנס לאתר ובקוד המקור שלו.
   כל עוד הוא ריק — הכפתור אינו מוצג כלל. */
var SupportConfig = {
  phone: '+972542117377'
};

/* ============================================================
   מתגי יכולות — כיבוי והדלקה בשורה אחת
   ------------------------------------------------------------
   יכולת שאפשר לוותר עליה יושבת כאן. שינוי הערך ל-false מכבה אותה
   לגמרי: הכפתורים נעלמים מהמסך, והפעולה עצמה חסומה גם אם מישהו
   יגיע אליה בדרך אחרת. אין צורך לגעת בשום קובץ נוסף.

   payboxImport — ייבוא תשלומים מקובץ של פייבוקס (או Excel / CSV),
   כולל פתיחת קובץ מוצפן בעזרת הסיסמה של המשתמש. כשהוא כבוי,
   הוספת תשלום נעשית ידנית בלבד, והספרייה החיצונית שמפענחת קבצים
   אינה נטענת לעולם.
   ============================================================ */
/* ============================================================
   הדומיין הרשמי של האתר
   ------------------------------------------------------------
   לכאן חוזרים הקישורים שנשלחים במייל. בלי הקיבוע הזה הכתובת
   נגזרת מהחלון שבו נרשמו, וכל פתיחה מכתובת אחרת — תצוגה מקדימה
   של Vercel, או www במקום הדומיין הראשי — הייתה שולחת את הקישור
   חזרה לשם. שתי התוצאות רעות: כתובת שאינה ברשימה המאושרת של
   Supabase נדחית, ומיילים שיוצאים עם דומיינים משתנים נראים פחות
   אמינים לשרתי הדואר.

   פיתוח מקומי (localhost) אינו מושפע — שם הקישור חוזר לאן שעובדים.
   ============================================================ */
var SiteConfig = {
  url: 'https://vaadhorim.com'
};

var Features = {
  /* כבוי: בוועד שהוזן בו רק מספר ילדים אין למה לשייך את התשלומים —
     התפריט מציע "ילד 1", "ילד 2", ואי אפשר לדעת מי מהם המשלם, ולכן
     הייבוא נעצר על אפס שורות. ראו הטיפול לפני הדלקה מחדש. */
  payboxImport: false
};

var CloudConfig = {
  url:   'https://pbqmbwxlumvrfgfzepdq.supabase.co',
  key:   'sb_publishable_hyFmuRw3lIGweNzgdrbYhw__tMGO-LF',
  table: 'vaad_state'
};
